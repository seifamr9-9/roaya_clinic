<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_lab_order_wizard_form" model="ir.ui.view">
        <field name="name">lab.order.wizard.form</field>
        <field name="model">lab.order.wizard</field>
        <field name="arch" type="xml">

            <form string="Create Lab Order">

                <sheet>

                    <group col="2">

                        <group string="Appointment Info">
                            <field name="appointment_id"/>
                            <field name="patient_id" readonly="1"/>
                            <field name="doctor_id" readonly="1"/>
                        </group>

                        <group string="Test Details">
                            <field name="test_type"/>
                            <field name="urgency"/>
                        </group>

                    </group>

                    <group string="Description">
                        <field name="description"/>
                    </group>

                </sheet>

                <footer>
                    <button name="action_confirm"
                            string="Confirm"
                            type="object"
                            class="btn-primary"/>

                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>

            </form>

        </field>
    </record>
</odoo>